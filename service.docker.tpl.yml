{{serviceName}}-{{version}}:
  environment:
    NODE_ENV: stage
    RDS_HOSTNAME: db
    REDIS_ACTIVE: 'false'
  labels:
    io.rancher.container.pull_image: always
  tty: true
  image: docker.entrecode.de/accountsrv:{{versionDots}}
  stdin_open: true
